#include<bits/stdc++.h>
using namespace std;
int n,x[100005],q,m[100005];
int main ()
{
	/* Input
	n = number of shops
	x[i] = price of drink in [i] shop
	q = number of days that want to buy a drink
	m[i] = money that can spent in [i] day */
	cin >> n;
	for(int i=1;i<=n;i++)
	{
		cin >> x[i];
	}
	cin >> q;
	for(int i=1;i<=q;i++)
	{
		cin >> m[i];
	}
	//
}
